# igx-stepper-layout

Minimal Angular + Ignite UI for Angular (IGX) test repository for a simple stepper layout case.

## Quick start

- `npm install`
- `npm start` to run locally
- `npm run build` to create a production build
